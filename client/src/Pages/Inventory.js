import React from 'react'

// only admins can access this page or see it on the header
export const Inventory = () => {
    return (
        <div>
            <h1>Inventory</h1>
        </div>
    )
}